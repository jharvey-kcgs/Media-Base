node_modules
.expo/
.DS_Store
*.log
npm-debug.*

# Holds the real token values as environment variables - this repo is
# public, so the real values stay local-only here. lib/config.ts itself
# is committed and contains no secrets now (it just reads these same
# variable names via process.env) - see .env.example for the tracked
# template/setup instructions, and lib/config.ts's own file header for
# why this changed from a git-ignored lib/config.ts to this.
.env
.env.local
